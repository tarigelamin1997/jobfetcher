"""JobFetcher — personal-scale serverless job-matching pipeline (evolutionary architecture).

The design lives in `docs/`; this package is the v0 implementation, built one unit at a
time. First unit: the silver `Dissector` (a JD -> structured contract) + the LLM client.
"""

__version__ = "0.0.0"
