# Ledger · Phase Index

> Live source of truth for progress. Status legend: ⬜ not started · 🚧 in progress · ✅ shipped (tagged release). `/start-step` sets 🚧, `/close-step` sets ✅ ([ADR-0013](../adr/0013-enforcement-gate-trio-branch-pr.md)). The roadmap beyond v0 is a **[living hypothesis](../03-roadmap.md)** — re-derived after each release; update this table as reality unfolds.

**Current state:** **v0.12.1 SHIPPED (2026-09-02) — the outage release.** The pipeline returned `statusCode: 500` on **every run from 2026-07-25 to 2026-09-01** and the alarms fired on all 38 days without being acted on ([ERR-010 → ERR-014](errors.md)); resolved + live-validated 2026-09-02 ([ADR-0036](../adr/0036-gold-filter-rejection-lineage.md) rejection lineage + migration `0007` · [ADR-0037](../adr/0037-per-task-reasoning-budgets.md) per-task reasoning budgets · [ADR-0038](../adr/0038-aurora-unencrypted-at-rest.md) the labeled encryption trade-off) — all five bulk reads keyset-paginated, 1,182 postings scored, digest sending again. Two units shipped between tags: the **public outcome-capture endpoint** ([INV-001](../investigations/INV-001-dark-feedback-loop/README.md) / [ADR-0035](../adr/0035-outcome-capture-endpoint.md), 2026-07-20) and the **silent-500 alarm** ([INV-002](../investigations/INV-002-silent-500-alarm/README.md), 2026-07-21 — the third alarm). **Previously: v0.12.0 (2026-07-17)** — full S3 audit persistence (every pipeline stage's results also land in S3 as batched JSONL: `silver/`/`gold/`/`scores/`/`runs/`) + a local Streamlit control panel (browse/curate/config), two agentic-squad units, both Examiner CLEAN PASS ([ADR-0032](../adr/0032-full-s3-audit-persistence.md) · [ADR-0033](../adr/0033-local-control-panel.md)); U1 live-validated on the deployed stack.** Earlier, v0.10.0 closed B-1 "reachable full-list from the digest" ([ADR-0030](../adr/0030-reachable-full-list-from-digest.md); 286 rows over the Data API, report page rendered + uploaded), and v0.11.0 fixed the scorer's boundary noise + honest graduations ([ADR-0031](../adr/0031-boundary-self-consistency-honest-graduations.md)). The pipeline runs itself unattended — v0.9.0's first solo flight (the 06:00 UTC cron) ran end-to-end and sent a digest with nobody watching. v0.7.0 "the pipeline remembers" (score lineage + outcome tracking + digest truthfulness), v0.8.0 scorer integrity, and v0.9.0 ops hardening + cold-start reliability were all built end-to-end by the agentic squad workflow. Nine P2-driven releases now sit on top of the v0.1 core, and what shipped diverged from the pre-drawn M1–M8 exactly as the living-hypothesis protocol predicts. The release arc since v0.1: **`v0.2.0` M1 "pipeline hardening"** (overruled the CV-tailoring hypothesis; ADR-0021) → **`v0.3.0` user-customizable settings + runtime config in S3** (ADR-0022; adopted patch-versioning) → **`v0.3.1` `employment_types`** (first patch release) → **`v0.4.0` reassess/replay** (delivers the old M4 graduation half early; ADR-0023) → **`v0.5.0` query/filter access via export** (ADR-0024) → **`v0.6.0` email-UX redesign**. Every release is **live-validated on the deployed stack** (Aurora scale-to-0 ⇒ ~$0 idle); **<!--fact:tests-->549<!--/fact--> collected (<!--fact:tests_unit-->482<!--/fact--> unit + <!--fact:tests_integration-->67<!--/fact--> integration) in CI (unit + integration + live-key skips), CI green.** **Shipped in v0.7.0: score_event lineage** — the first agentic-workflow unit, built + adversarially reviewed + integration-verified on branch `feat/score-event-lineage` ([ADR-0025](../adr/0025-score-event-lineage.md); row below) — **and outcome tracking + human-override lineage** — Squad Run 2 ([ADR-0026](../adr/0026-outcome-tracking-override-lineage.md); row below). **Live-validated at release (2026-07-08): 771 lineage events through the new dual-write over the Data API, 228 baseline events intact, `.rowcount` override clean, 10 graduations standing (ERR-008 logged: client-side CLI retry).** **All shipped 2026-07-10 (live-proven by the first unattended 06:00 UTC cron): v0.8.0 scorer integrity (Squad Run 4 — migration 0006, subscores + shadow code-total — [ADR-0028](../adr/0028-scorer-subscores-shadow.md)) + v0.9.0 ops hardening + cold-start reliability (Squad Runs 5–6 — S3 remote state · dead-man/errors alarms · the `{"mode":"smoke"}` deploy gate · the Aurora resume-wait, [ADR-0029](../adr/0029-ops-hardening.md) / [ERR-009](errors.md)); the digest-truthfulness unit (Squad Run 3, [ADR-0027](../adr/0027-digest-truthfulness.md)) shipped inside the v0.7.0 tag tree (merged just before the tag).** **Since then: `v0.10.0` reachable full-list (B-1, [ADR-0030](../adr/0030-reachable-full-list-from-digest.md)), `v0.11.0` scorer boundary self-consistency + honest graduations ([ADR-0031](../adr/0031-boundary-self-consistency-honest-graduations.md)), and `v0.12.0` full S3 audit persistence + a local control panel ([ADR-0032](../adr/0032-full-s3-audit-persistence.md) · [ADR-0033](../adr/0033-local-control-panel.md)) — all agentic-squad units.** **Next migration is re-ranked by P2 from real use** — leading still-open candidates: multi-source + clustering dedup (M2, Adzuna), Step Functions/chunking for the full backfill (M3), the Notion workspace / near-miss half of M4, and CV tailoring (the pre-drawn M1, re-queued). *(v0.2 M1 detail + v0.1.0 history below.)*

**v0.2.0 SHIPPED (2026-07-02) — M1 "pipeline hardening" (the P2 protocol's actual first migration, overruling the CV-tailoring hypothesis).** Ran the tool live on the full 18-query GCC sweep, measured three bottlenecks + a zombie-retry hazard, fixed all as three gate-trio units (H-1 retry+jitter & symmetric isolation · H-2 in-Lambda concurrency + deadline guard + `retry=0` IaC + mem 1024 · H-3 subset-title gold filter + selectable LLM filter), and **re-validated live on the exact ~132-posting backlog the pre-fix code died on** → `statusCode 200`, backlog fully drained, **0 run-fatal errors** (15 dissect + 0 score failures isolated), and a **populated 21-job digest sent** (GCC DE roles scored 60–95). Measured: **~13× throughput** (~1.1→~14–15 dissections/min), **no timeouts**, junk titles eliminated, `MaximumRetryAttempts=0` Terraform-managed ([ADR-0021](../adr/0021-m1-pipeline-hardening.md); ERR-006/007). *(v0.1.0 history below.)*

**v0.1.0 SHIPPED (2026-06-29) — deployed to AWS, validated live end-to-end, then torn down to ~$0.** The full pipeline ran for real: `terraform apply` → the 14-resource stack, schema migrated via `alembic upgrade head` over the Data API, invoke → `statusCode 200` → **fetched 10 → bronzed 10 → silvered 8 → gold 8 → scored 8 → notify sent** on real UAE Data-Engineer postings; **two emails delivered (SES 0 bounces)** — a VG5 zero-path digest and, on an idempotent re-run (`already: 8` skipped — **VG4 live**), a VG5 matches-path 7-job shortlist; the live run caught **2 Data-API deploy-only bugs** (`migrations/env.py` `%`→`%%`, `handlers/pipeline.py` `cluster_arn`→`aurora_cluster_arn`) + a Lambda timeout 300→900s, all fixed (PR #13); then **`terraform destroy` → 14 destroyed**, verified GONE, back to ~$0 (Secrets Manager keys preserved). **Scale finding (a real P2 bottleneck):** the single Lambda fits the daily incremental run (~10–30 jobs) but **can't** run the full 18-query × 30-day backfill inside the 15-min max → reinforces **M3 (Step Functions)**. **Next = the bottleneck (P2) protocol → pick M1.** *(History below — the v0 build arc.)* Design + docs complete. First code existed: a validated, `SearchSpec`-driven JSearch probe ([`scripts/jsearch_probe.py`](../../scripts/jsearch_probe.py)) proven end-to-end against the live API; JSearch key in Secrets Manager; **LLM = OpenAI-compatible API, v0 backend = DeepSeek** ([ADR-0017](../adr/0017-llm-transport-openai-compatible-deepseek.md)) — **✅ verified live 2026-06-24** (`deepseek-v4-flash`, HTTP 200); Bedrock's wall ([ERR-001](errors.md)) is **worked around: no open blocker.** C-1 (silver `Dissector`), C-2 (storage schema + `Repository`), C-3 (Terraform infra — **apply-validated end-to-end**: provisioned, the Data API confirmed live, then destroyed back to ~$0), **Step 4** (fetch + bronze→silver landing), and **Step 4b — Unit A** (the `Profile` contract + the gold filter: a `FilterStrategy` port with a **deterministic default** and an optional LLM strategy, fits getting a **1:1 cluster** `cluster_id = posting_id`), and **Step 5 — Unit B** (the Scorer: a grounded temp-0 **7-factor ATS** `Scorer` → `ScoreResult`, `score_gold` orchestrator, `score` rows keyed on a now-`UNIQUE` `cluster_id` via migration **0002**, `fit_category` band-derived at runtime [VG8 threshold-is-config], **VG3 determinism softened to best-effort** — temp 0 is sent but `deepseek-v4-pro` is non-deterministic even at temp 0, so precise calibration is deferred to M7), and **Step 6** (the Notifier: the `Notifier` port + `SesNotifier` + a dependency-free `render_digest` [HTML + plaintext] · apply links scheme-allowlisted to http/https · `Repository.get_scored_shortlist` · the `notify` orchestrator · **VG5** zero-matches "no matches today" email · terraform `recipient_email`), and **Step 7** (the single Lambda `handler` wiring `ingest → gold → score → notify` behind one entry point — the data path is now wired **end-to-end**: seeds the `profile` row, threads the correlation `run_id`, resolves the DB engine + config paths from env, a stage failure → `500` → next run resumes; **VG4 idempotent** via upserts + a **`run_log` send-once guard** [migration 0003, PK `(run_date, user_id)`] — the email is **at-least-once** since the SES↔`run_log` dual-write can't be atomic; fixed the latent `DATA_BUCKET → JOBFETCHER_DATA_BUCKET` env-name crash) are built, and **Step 8** (the test round-up — the v0 pyramid complete + green: **180 unit + ~26 integration + ~3 live**, `ruff` clean, **89% full-suite coverage**; closed the two CI-invisible gaps — a **VG3 offline negative** [the client must send the configured temp → temp-≠-0 regression caught without a key] and the **`SearchSpec` contract negatives** [9 tests; VG1 + VG8 config layer]; added [`tests/README.md`](../../tests/README.md) — the **VG1–VG8 → test traceability map** — and `pytest-cov` tooling) are built, and **Step 9** (minimal CI — GitHub Actions on PR→main + push→main: **3 jobs** — `ruff` + `alembic upgrade head` + `pytest --cov --cov-fail-under=85` against a `postgres:16-alpine` service [live DeepSeek/JSearch tests skip without keys] · `terraform validate` [AWS-free] · **gitleaks** secret-scan; **VG7 now enforced** via gitleaks in pre-commit **and** CI — passes clean, **blocks a planted fake key**; `ruff-format` deferred to a separate one-time reformat; once the PR's CI is green these become `main`'s **required status checks**) is built; **test suite green** (unit + integration on moto S3 + local Postgres), and **Step 10** (deploy + first live run → **tag `v0.1.0`, 2026-06-29**) shipped: the 14-resource stack applied, the pipeline run + validated end-to-end on real AWS, the 2 Data-API deploy bugs fixed (PR #13), then destroyed to ~$0. **All 10 v0 steps shipped. Next = the bottleneck (P2) protocol → M1** (the full 18-query backfill needs **M3**).


> **Counts here are now checked by CI, not trusted.** Test totals, resource counts and release counts drifted repeatedly — the ERR-010 close-out found "21 resources" when there were 32, "2 alarms" when there were 3, "466 tests" when there were 512, and the *corrected* figures were stale again within two days. This file used to carry a warning telling you not to trust its own numbers, which is a symptom, not a cure. Since [ERR-016](errors.md), the numbers that matter are **guarded**: wrapped in a non-rendering `<!--fact:name-->` marker and diffed against their ground-truth command by [`scripts/check_docs.py`](../../scripts/check_docs.py) in CI, so a stale one fails the build. Run `python scripts/check_docs.py --list` for the live values.
>
### Verified against the deployed stack — 2026-09-04

Read-only AWS calls as `jobfetcher-dev` (us-east-1). Every row below is a doc claim confirmed live, so a future session knows what was checked rather than re-deriving it:

| Doc claim | Live |
|---|---|
| 2 Lambdas; pipeline 1024 MB / 900 s | ✅ `jobfetcher-dev-pipeline` 1024/900, `jobfetcher-dev-capture` 256/30 |
| Capture endpoint is a public Function URL | ✅ `AuthType: NONE` (URL deliberately not recorded here) |
| 3 CloudWatch alarms | ✅ dead-man · errors · returned-500, all `OK` |
| Aurora SLv2, scale-to-0, Data API | ✅ `MinCapacity 0.0`, `MaxCapacity 2.0`, `HttpEndpointEnabled: true` |
| **Unencrypted at rest** ([ADR-0038](../adr/0038-aurora-unencrypted-at-rest.md)) | ✅ `StorageEncrypted: false` — the labeled trade-off is accurate, not aspirational |
| Major-only engine pin ([ERR-012](errors.md)) | ✅ `16.11` — AWS owns the minor, exactly as intended |
| Daily 06:00 UTC cron | ✅ `cron(0 6 * * ? *)`, ENABLED |
| 7 S3 prefixes (`raw` `config` `reports` `runs` `silver` `gold` `scores`) | ✅ all present |
| `wait_for_db_resume` ([ERR-009](errors.md)) | ✅ **observed working** — 3 resume waits against the 90 s budget in the 2026-09-03 run, then a clean pass |

**Corrected by this check:** the account holds **four** Secrets Manager secrets, not the two the docs named — Terraform also owns `jobfetcher/capture-token` and the `rds!cluster-…` master password (`manage_master_user_password = true`), which both Lambdas read via `$DB_SECRET_ARN`.

> ⚠️ **This check also found a live regression that is NOT documentation debt:** since 2026-09-02 every run returns `200` with `fetched: 0` and no `raw/` objects have landed — the pipeline is green and ingesting nothing, and no alarm covers that shape. Owned by the ingestion work, tracked separately from this ledger.

> An **unguarded** number here is a *historical measurement* — true as of the date beside it (e.g. "286 rows over the Data API, 2026-07-10"), and correct forever in that sense. Those are deliberately not automated; they are attributed instead.

| Release | Adds | Status |
|---|---|---|
| **v0.1** | One Lambda: 1 source → S3 + Postgres → LLM score (DeepSeek) → daily email; Terraform, Secrets Manager, tests, minimal CI | ✅ **shipped — tag `v0.1.0`, 2026-06-29** (deployed to AWS · validated live end-to-end · destroyed to ~$0).<br/>Build arc: Step 0 done; **C-1** silver `Dissector` built + live-validated; **C-2** storage schema + `Repository` built + **live-validated** (Alembic migration · SQLAlchemy + aurora-data-api · ADR-0018; **5/5 DB round-trip passes on real Postgres** — schema builds, `DissectedPosting` round-trips equal, idempotency holds); **C-3** Terraform infra **apply-validated** (14 resources — Aurora SLv2 scale-to-0 + Data API, S3, least-privilege IAM [no Bedrock], stub Lambda, EventBridge, SES — applied → **Data API live on PG 16.6 over HTTPS** · scale-to-0 confirmed → destroyed to ~$0; real deploy at Step 10); **Step 4** fetch + bronze→silver landing built (`SourceAdapter`/`JSearchSourceAdapter` · S3 raw store · clean/fingerprint · `ingest`); **Step 4b (Unit A)** gold filter + `Profile` built (`FilterStrategy` port + `DeterministicFilterStrategy` [v0 default] / `LlmFilterStrategy` [config-selectable] · `Profile` contract · `apply_gold_filter` → 1:1 clusters `cluster_id = posting_id` · 5 new `Repository` methods); **Step 5 (Unit B)** Scorer built (7-factor ATS `Scorer` → `ScoreResult`, temp 0 · `score_gold` orchestrator · `get_gold_candidates`/`save_score`/`mark_scored` · `derive_fit_category` band routing · `UNIQUE(score.cluster_id)` migration 0002 → idempotent `on_conflict_do_update` · **VG8** threshold-is-config · **VG3 best-effort** — temp 0 sent, model non-deterministic at temp 0, calibration → M7); **Step 6** Notifier built (`Notifier` port + `SesNotifier` [boto3 SES, HTML + plaintext UTF-8] · dependency-free `render_digest` — matches `score >= threshold` [score/title/company/apply-link/one-line-why] + "+N below threshold" footer, all text HTML-escaped, apply-link `href` scheme-allowlisted to http/https · `Repository.get_scored_shortlist` [score↔posting JOIN on `cluster_id`] · `notify` orchestrator [single threshold authority, send-failure raises loud] · **VG5** zero-matches "no matches today" email · terraform `recipient_email` env/var/output — sandbox-verify the recipient · send-idempotency deferred to Step 7); **Step 7** single Lambda `handler` built (`handlers/pipeline.py` wiring `ingest → apply_gold_filter → score_gold → notify` · seeds the `profile` row · correlation `run_id` in structured logs · env-resolved DB engine [local URL vs Aurora Data API] + config paths · `{statusCode, run_id, …counts}` summary, stage failure → `500` → resume · **`run_log` send-once guard** [migration 0003, PK `(run_date, user_id)`] + `upsert_profile`/`was_digest_sent`/`mark_digest_sent` · **VG4** idempotent re-run + ≤1 email — **at-least-once** email [the SES↔`run_log` dual-write isn't atomic; transactional outbox = the scale-up] · terraform handler entry-point + `SEARCH_CONFIG_PATH`/`PROFILE_PATH` env + the `DATA_BUCKET → JOBFETCHER_DATA_BUCKET` fix; packaging deferred to Step 10); **Step 8** test round-up built (the v0 pyramid complete + green: **180 unit + ~26 integration + ~3 live**, `ruff` clean, **89% full-suite coverage** [unit-only ~81%]; **VG3 offline negative** `test_temperature_from_config_is_in_request_payload` [CI-enforces temp-≠-0 without a key] + **`SearchSpec` contract negatives** [9 tests — VG1 + VG8 config layer]; [`tests/README.md`](../../tests/README.md) **VG1–VG8 → test matrix**; `pytest-cov` + `[tool.coverage]` tooling, opt-in `--cov`; VG2/VG4/VG5/VG8 + all 5 unit-pyramid items fully covered; **VG7** secret-scan deferred to Step 9, **VG6** teardown an infra gate, live-smoke at Step 10); **Step 9** minimal CI built (GitHub Actions on PR→main + push→main · 3 jobs — **lint-and-test** [`ruff` + `alembic upgrade head` + `pytest --cov --cov-fail-under=85`, `postgres:16-alpine` service; live tests skip without keys] · **terraform-validate** [`init -backend=false` + `validate`, AWS-free] · **secret-scan** [gitleaks] · `.pre-commit-config.yaml` gitleaks + ruff · **VG7 enforced** — gitleaks pre-commit + CI, blocks a planted fake key · `ruff-format` deferred · once the PR is green → `main` **required status checks**); **test suite green** on moto S3 + local Postgres; **Step 10** deploy + first live run shipped — 14-resource stack applied, pipeline validated end-to-end on real AWS (`statusCode 200` · fetched 10 → … → notify sent · 2 emails, SES 0 bounces · VG4 + VG5 live), 2 Data-API deploy bugs fixed (PR #13), then destroyed to ~$0 → **tag `v0.1.0`** |
| **M1 · v0.2** | **Pipeline hardening** — LLM retry+jitter & symmetric isolation (H-1) · in-Lambda `ThreadPoolExecutor` concurrency + deadline guard + `retry=0` IaC + mem 1024 (H-2) · subset-title gold filter + `$GOLD_FILTER_STRATEGY` (H-3) | ✅ **shipped — tag `v0.2.0`, 2026-07-02** (P2-derived, overrode the CV hypothesis; re-validated live: ~13× throughput · 0 crashes · junk eliminated · 21-job digest sent — [ADR-0021](../adr/0021-m1-pipeline-hardening.md)) |
| **v0.3.0** | **User-customizable settings + runtime config in S3** — 3 strictness knobs as user config (profile row re-syncs from config every run, fixing the write-once trap) · config YAMLs moved from the Lambda zip into S3, read at runtime → `scripts/push_config.py`, **no redeploy** · adopted patch-versioning | ✅ **shipped — tag `v0.3.0`, 2026-07-03** ([ADR-0022](../adr/0022-runtime-config-in-s3.md)) |
| **v0.3.1** | **`employment_types`** validated as an enum + wired through to JSearch — first patch release | ✅ **shipped — tag `v0.3.1`, 2026-07-03** |
| **v0.4.0** | **Reassess / replay** — `{"mode":"reassess"}` re-scores existing jobs vs the updated profile with **zero re-fetch** (immutable-bronze replay); jobs **graduate** (stretch→strong_fit), `previous_score` tracks before→after. *Delivers the old M4 graduation half early.* Live: 180 reassessed, 15 graduated, bronze unchanged | ✅ **shipped — tag `v0.4.0`, 2026-07-06** ([ADR-0023](../adr/0023-reassess-replay.md)) |
| **v0.5.0** | **Query / filter access** — `scripts/export.py` → SQLite/CSV snapshot (flat jobs table + bronze/runs/profile) opened in Datasette / DB-Browser / Excel; no custom UI | ✅ **shipped — tag `v0.5.0`, 2026-07-06** ([ADR-0024](../adr/0024-query-via-export.md)) |
| **v0.6.0** | **Email UX** — daily digest redesigned into scannable cards, prominent **Apply** button + location per job; `scripts/preview_digest.py` preview | ✅ **shipped — tag `v0.6.0`, 2026-07-06** |
| **v0.7.0 · score lineage** *(shipped 2026-07-08)* | **Append-only `score_event` history + lineage** — `save_score` dual-writes the `score` upsert + an immutable event (`scoring_model`/`profile_hash`/`run_id`) in one transaction; migration 0004 + the `'pre-0004'` baseline backfill rescues the 180 existing scores; `profile.profile_hash`; **reassess age bound** (`get_scored_for_reassess(max_age_days)` + REQUIRED `reassess_max_age_days` knob — **⚠️ push config before deploy**); export gains `score_events` | ✅ **SHIPPED in v0.7.0 (2026-07-08)** — live-verified: migrations applied over the Data API, 228 baseline events backfilled, 771 events through the dual-write ([ADR-0025](../adr/0025-score-event-lineage.md)) |
| **v0.7.0 · outcome tracking** *(shipped 2026-07-08)* | **Append-only `application_event` outcome log + human score overrides** — migration 0005 (`applied`/`interview`/`offer`/`rejected`/`withdrawn`; the CHECK built from the single `APPLICATION_STATUSES` vocabulary; no backfill; re-record = new event, latest wins); **`scripts/track.py`** (record outcomes · `find` · `events` · `override`); `set_score_override` = ONE transaction updating `score.score_override` **+ appending a `score_event` with `scoring_model='human-override'`** (overrides join the lineage log — nothing erased; an override survives re-scores and never rewrites `score.fit_category`); export gains `score_override` + latest-status columns + an `application_events` table. **⚠️ `alembic upgrade head` before first `track.py`/new-export use** | ✅ **SHIPPED in v0.7.0 (2026-07-08)** — live-verified: `track.py override` clean (first `.rowcount` over the Data API), human-override event in the lineage log, export carries all six tables ([ADR-0026](../adr/0026-outcome-tracking-override-lineage.md)) |
| **digest truthfulness** *(shipped in v0.7.0's tag tree)* | **The digest tells the truth** — **"New since last digest"** leads (NEW iff the first-ever digest, or a fresh judgment — `scored_at` vs `MAX(run_log.digest_sent_at)` — that is actually news: a first scoring or a graduation `previous < threshold <= score`, badged green `↑ old→new`); everything else compacts into **"still open"** (count + top-5 + "…and n more — see your export"); same-fingerprint dups (**measured 17.8%** of scored postings) collapse render-time to one card (`seen n× — scores lo–hi`, one section per group); a zero-new day sends an honest "no new matches since {date}"; new REQUIRED **`digest_max_age_days`** knob ages stale matches out (the ADR-0025 COALESCE pattern — **⚠️ push config before deploy**); new `get_last_digest_sent_at`; **no migration**. Cluster-attach dedup explicitly deferred to M2 | ✅ **shipped in v0.7.0's tag tree (2026-07-08; live-validated 2026-07-09/10)** (branch `feat/digest-truthfulness`; the **first auto-pilot unit** — severity-gated policy of 2026-07-07: Claude merges on a clean Examiner pass + green CI; the Examiner **contested F1** — the `previous_score`-only newness rule that would have re-presented every old job as "New" daily — adjudicated fix-now, fixed in `0eca0f7`, **re-verified CLEAN PASS**; 311 unit + 39 integration green, 95.02% full-suite coverage — [ADR-0027](../adr/0027-digest-truthfulness.md)) |
| **v0.8.0 · scorer integrity — subscores + shadow totals** *(shipped 2026-07-10)* | **The 7-factor formula finally exists** — measured same-input drift up to **27+** (live lineage: one job scored 35–88 across runs) against a near-miss band of 10, and the weights existed *nowhere* (the model freelanced the weighting since v0.1). `ScoreResult` gains 7 optional bounded subscores; **`FACTOR_WEIGHTS`** (.30/.20/.15/.15/.10/.05/.05, Tarig's PDF-derived formula, module-load sum==1.0); the prompt demands `score` FIRST (anti-anchoring) + "not a mechanical average"; **SHADOW mode** — the LLM holistic stays the product number everywhere, `compute_code_total` (banker's rounding) is only logged (WARNING \|delta\|>20) + persisted (**`subscores` JSONB** on `score`/`score_event`, migration **0006**; blob `{7 factors, code_total, llm_total}` or NULL, never partial; re-score replaces the current-view blob); omitted → fail-open, malformed → fail-closed; reassess gains **`delta_buckets`/`max_delta`/`mean_delta`** (the variance + prompt-drift instrument); M7 cut-over criterion stated. Rode along: the `migrations/env.py` `fileConfig` logger-reset fix. **⚠️ migrate 0006 BEFORE deploy — run-fatal class** | ✅ **shipped — tag `v0.8.0`, 2026-07-10** (Examiner CLEAN PASS, zero blocking; S1+N6 applied) (branch `feat/scorer-subscores`, commits `168ab39`+`33a8ef4`+`cda34b9`; **CRUCIAL tier — PR to Tarig**, not auto-pilot; 346 unit + 42 integration green, full suite 388 passed, 95.16% coverage — [ADR-0028](../adr/0028-scorer-subscores-shadow.md)) |
| **v0.9.0 · ops hardening + reliability** *(shipped 2026-07-10)* | **S3 remote state + real alarms + a deploy smoke gate** — Terraform state moves to a deliberately **unmanaged**, versioned S3 bucket (`use_lockfile` — TF ≥ 1.10 native locking, no DynamoDB; the state must survive `terraform destroy`; **ONE-TIME human-present migration** = [runbook §3](../runbooks/deploy.md)) · **two CloudWatch alarms → one SNS topic → email**: a **dead-man** on the daily EventBridge rule (Invocations, `RuleName` dim, 86400 × 1 `Sum < 1`, missing = breaching — worst-case detection ~2 days, documented honestly) + **Lambda Errors** (3600 × 1 `Sum >= 1`, notBreaching; returned `statusCode`-500s not counted — future custom metric); both notify recovery; SNS confirmation click = runbook §4 · **`{"mode":"smoke"}`** post-deploy gate (Data-API connect + `alembic_version` vs `$ALEMBIC_HEAD` [staleness-pinned fallback `0006_subscores`]; 200/400/500; **zero side effects** — proven by zero-rows-across-8-tables integration tests with exploding fakes; the ERR-004/005 watch-item cycle becomes a mechanical gate) · `scripts/invoke.py` (ERR-008 wrapper) **explicitly decided AGAINST** — procedure-only | ✅ **shipped — tag `v0.9.0`, 2026-07-10** (Examiner CLEAN PASS, zero blocking; S1+N1 applied in `a359431`; live-proven by the first unattended cron) (branch `feat/ops-hardening`, commits `7b724ce`+`09335b2`+`2c48b1b`+`a359431`; **CRUCIAL tier — PR to Tarig**; 349 unit + 45 integration green, full suite 394 passed, 95.21% coverage — [ADR-0029](../adr/0029-ops-hardening.md)) |
| **v0.10.0 · reachable full-list** *(shipped 2026-07-10)* | **B-1 "reachable full-list from the digest"** — `notify` builds a **single self-contained HTML page** of ALL scored jobs (surfaced + still-open + below-threshold; sortable/filterable, no framework/CDN) → S3 `reports/{run_date}/jobs-{run_id}.html` → a **presigned https URL** that REPLACES the digest's two former dead-text lines ("…and n more — see your export" + "+N below threshold"); wrapped in a **NON-FATAL guard** (report failure degrades to plain text, never blocks the send). New `core/report.py` (pure render) + `adapters/s3_reports.py` (`S3ReportStore`) + `Repository.get_all_scored`; **no migration, no Terraform/IAM change, no new dependency**. B-1 is now **CLOSED** (was a backlog item); only **B-2** (Gmail-spam deliverability, blocked on a sender domain) remains open | ✅ **shipped — tag `v0.10.0`, 2026-07-10** (Examiner **CLEAN PASS** → auto-merge PR #30 — the first bottleneck solved end-to-end by the autonomous agentic squad; 385 unit green; live-validated: `get_all_scored` = **286 rows** over the Data API, the ~242 KB report page rendered + uploaded — [ADR-0030](../adr/0030-reachable-full-list-from-digest.md)) |
| **v0.11.0 · scorer — boundary self-consistency + honest graduations** *(shipped 2026-07-11)* | **The shortlist boundary stops being a coin-flip** — P2-scan measured the LLM scorer's same-input drift (avg spread **15.95**, max 60; **62/286** scores within ±16 of the cut) with reassess "graduation" badges firing on that noise (15 false positives, unchanged profile). **Boundary resample** (`_score_with_resample`): a first score within `RESAMPLE_TRIGGER_MARGIN` (16) of threshold → re-scored to `RESAMPLE_N` (3), **median sample** kept (coherent, never averaged); clearly-in/out or N≤1 → one call (cost guard); **deadline-aware**; wired into `score_gold` + `reassess`; module knobs, **no required `SearchSpec` field**, `Scorer` stays pure. **Honest graduations**: a crossing is a graduation only when the profile actually changed (prior `score_event` hash ≠ current) — reassess + digest sides; same-profile crossing → `unchanged`. M7 code-total cut-over **overturned with evidence** (inherits subscore noise). **No migration / infra / dep** | ✅ **shipped — tag `v0.11.0`, 2026-07-11** · **CRUCIAL tier** (Investigator P2 scan → Surgeon → Examiner **CLEAN PASS** + deadline should-fix; reassess integration test reconciled; CI green 446 passed / 5 skipped) · **live-validated:** smoke `200 @ 0006_subscores`, reassess **163 re-scored → `graduated: 0`** (honest-graduation gate holds under the unchanged profile), `mean_delta` **8.4** vs the ~16 baseline (78% within 10 pts); full-backlog reassess now deadline-partial (163/123 — backlog B-4) — [ADR-0031](../adr/0031-boundary-self-consistency-honest-graduations.md) |
| **v0.12.0 · U1 — full S3 audit persistence** *(deployed 2026-07-17)* | **Every pipeline stage's procedures + results also land in S3** — new `S3AuditStore` writes batched JSONL (`silver/`/`gold/`/`scores/{run_date}/{run_id}.jsonl`, scores from score_gold + reassess) + `runs/{run_date}/{run_id}.json` (the run summary that was logs-only). **Non-fatal by contract** (`_guarded_put` wraps serialize + put — an audit write can never fail a run; even construction guarded); additive (four `audit_store=None` hooks); main-thread accumulation (H-2); smoke writes nothing. **No migration/IAM/dep.** | ✅ **U1 shipped + deployed (PR #32, 2026-07-17)** · Examiner **CLEAN PASS** (6 invariants confirmed); live-validated (`runs/` + `gold/` objects on the deployed stack); CI green — [ADR-0032](../adr/0032-full-s3-audit-persistence.md) |
| **v0.12.0 · U2 — local control panel** *(shipped 2026-07-17)* | A local Streamlit app (`streamlit run scripts/panel.py`, no hosting/auth/deploy) — **Browse** (filter every scored job, reuses `export.read_data`), **Curate** (override a score / record an outcome, reuses the extracted `track.apply_override` + `track_application_event`), **Config** (edit search params → `validate_config_text` → push to S3). `streamlit` an optional `[panel]` extra, never in the Lambda. Non-crucial, no deploy | ✅ **shipped (PR #33, 2026-07-17)** · Examiner **CLEAN PASS** (behavior-preserving extractions; the config form's all-required contract; validate-is-the-gate); 414 unit green; panel UI manual/smoke — [ADR-0033](../adr/0033-local-control-panel.md). **→ tagged `v0.12.0`.** |
| **INV-002 · silent-500 alarm** *(shipped 2026-07-21)* | **A returned `statusCode:500` now pages** ([INV-002](../investigations/INV-002-silent-500-alarm/README.md): a returned 500 is a *successful* Lambda invocation → invisible to the Errors alarm; 0 alerts on the 2026-07-09 missed digest, [ADR-0029](../adr/0029-ops-hardening.md) gap). The handler emits a **mode-gated `PIPELINE_ALARM`** marker (unattended daily only; smoke/reassess excluded → no false-fire); `alarms.tf` adds a managed log group + a log-metric-filter → `PipelineReturned500` → an alarm reusing the SNS topic. No migration/dep | ✅ **merged (PR #35) → deployed + live-validated 2026-07-21** (smoke `200`; `describe-alarms` `State=OK` wired to SNS; filter present; `test-metric-filter` matches only the marker). Squad: Investigator dossier → Surgeon → Examiner **CLEAN PASS** → CodeRabbit VG-runnability nit applied; **455 passed**. **No per-unit tag** (batches into `v0.12.x`) |
| **INV-001 · outcome-capture endpoint** *(shipped 2026-07-20)* | **The dark feedback loop closes** ([INV-001](../investigations/INV-001-dark-feedback-loop/README.md): `application_event` = 0 rows live) — a public **Lambda Function URL** the digest/report "✓ Mark applied" links hit, guarded by a short-lived **HMAC token** scoped to `{posting_id,status}` (key Terraform-owned); `verify` before any DB touch (forged/expired → 0 rows), reuses `track_application_event`. Rung 2 (human-approved, checkpoint A). No migration/dep | ✅ **merged (PR #34) → deployed + live-validated 2026-07-20** (smoke `200`; capture endpoint: forged/expired/no-token → 400, valid-token-unknown-posting → 404/0-rows). Squad: Investigator dossier → Surgeon → Examiner **CLEAN PASS** → CodeRabbit correlation-ID applied; **451 passed**, CI green. **No per-unit tag** (batches into `v0.12.x`) — [ADR-0035](../adr/0035-outcome-capture-endpoint.md) |
| **ERR-010/011/013 · the 38-day outage** *(fixed + live-validated 2026-09-02)* | **The pipeline had returned `statusCode:500` on EVERY daily run since 2026-07-25 — 38 consecutive days, last digest 2026-07-24 — and the repo could not have shown it.** Found by querying the deployed stack during an open-ended code review, not by the alarm: INV-002's detector fired all 38 days and delivered two SNS emails a day, which had become indistinguishable from the product. Three independent faults sharing one symptom (`silvered: 0`): (1) **the gold-filter read crossed the Data API's 1 MB cap** — `select(tables.posting)` unbounded, 1,022 rows / 3,110 kB live, of which 1,143 kB was a never-read `description`; [ADR-0014](../adr/0014-operational-store-aurora-serverless-data-api.md) had dismissed this cap as *"irrelevant at 10–30 rows/day"*, sizing a **stock by its flow**; (2) **DeepSeek out of credit** (402, an operator action); (3) **reasoning tokens consuming the entire `max_tokens`**, returning HTTP 200 with empty content that `content or ""` turned into a JSON parse error. Fixes: column projection + **rejection lineage** (migration **0007**, [ADR-0036](../adr/0036-gold-filter-rejection-lineage.md)) bounding the read *without* freezing gold decisions; **keyset pagination on all five bulk reads** after the first fix was sized in the wrong units ([ERR-013](errors.md): `pg_column_size()` is compressed on-disk, the Data API sends uncompressed — `skills` is 537 kB vs **1,291 kB**); **per-task reasoning budgets** ([ADR-0037](../adr/0037-per-task-reasoning-budgets.md)); `LlmBillingError` counted in both stages | ✅ **fixed, deployed + live-validated 2026-09-02** — final run `{'gold': 618, 'scored': 618, 'failed': 0, 'deferred': 0}`, `partial: False`; **1,182 scored / 260 rejected / nothing pending**; digest sent (SES `Send: 1`, 0 bounces), the first since 2026-07-24. 547 tests green. PRs #36, #39, #40, #43 |
| **ERR-012 · Terraform was downgrading the database** *(fixed 2026-09-02)* | `aurora.tf` pinned `engine_version = "16.6"` while `auto_minor_version_upgrade = true`; AWS had patched to **16.11**, so **every apply — any routine deploy included — attempted an unsupported minor downgrade** and blocked the outage fix. `-target` did not help (the Lambda depends on the cluster ARN). Fixed with major-only `"16"` **plus** `lifecycle { ignore_changes = [engine_version] }` — the major-only pin alone still planned `16.11 -> "16"`, caught by **reading the plan**. Major upgrades now require lifting the ignore ([runbook §6](../runbooks/deploy.md)) | ✅ **fixed 2026-09-02** — PRs #37, #38. Rule: an attribute the provider may change on its own must be un-asserted or ignored, never both asserted and delegated |
| **ERR-014 + B-10 · what the tool actually costs and what actually limits it** *(measured 2026-09-02)* | **The documented DeepSeek limit was never about this account.** `deepseek-v4-pro`'s published **500 concurrent** is a ceiling; the effective limit is **scaled to remaining balance** and is stated *only* in a 429 body — no `/limits` endpoint, no `x-ratelimit-*` headers. Measured **39** near-empty, **≥200** at $10 (bursts of 60/120/200, zero 429s — a deliberate cheap overshoot as the measurement instrument). **B-10, cost measured via `GET /user/balance` either side of one run: 618 scorings, $9.71 → $1.48 = $8.23 ⇒ $0.0133/posting ⇒ ~$4–12/month.** Overturns the standing *"scale-to-0 ⇒ ~$0 idle"* framing: **AWS is the cheap part; DeepSeek is essentially the whole running cost**, 80–85% of it reasoning tokens that are discarded — and balance is a **capacity input**, not just a bill | 📋 **figures established, no instrumentation built** (Tarig: be aware of it now, build later). Concurrency reverted to 8; `lambda.tf` records that a safe value is a function of that day's balance. PRs #41, #42, #44, #45 |
| **B-5 · a daily alarm is not an alarm** *(logged 2026-09-02)* | The INV-002 detector worked perfectly — fired on all 38 outage days, two SNS emails daily, reaching the operator's inbox — and changed nothing, because a signal arriving every morning at the hour the digest used to is indistinguishable from the product. **Nothing escalates a *persistent* alarm differently from a one-off.** Deliberately **no new alarm added**: more alarming treats the wrong failure | ⬜ **open** — recorded in the [backlog](backlog.md) with the design space (escalate on persistence; alarm on the *absence of product* i.e. SES `Send == 0`; stop the OK-half doubling the volume) |
| M2 · vNext? | Multi-source + clustering dedup + Suspected-Duplicates (Adzuna candidate) | ⬜ (hypothesis) |
| M3 | Single Lambda → Step Functions / chunking (evidenced: full 18-query backfill > 15-min max) | ⬜ (hypothesis) |
| M4 *(partly shipped)* | Notion workspace + near-miss surfacing *(graduation half shipped early as v0.4.0)* | ⬜ (hypothesis) |
| M5 | dbt marts on Postgres | ⬜ (hypothesis) |
| M6 | Skill-Demand + Sector Intelligence | ⬜ (hypothesis) |
| M7 | Right-sized observability + scoring calibration loop | ⬜ (hypothesis) |
| M8 · v1.0.0 | CI/CD hardening + README/diagram/demo + seam-ready stubs | ⬜ (hypothesis) |
| M-later | CV tailoring (reliable renderer, draft→review) — re-queued from the pre-drawn M1 slot | ⬜ (hypothesis) |
| Future | Debezium CDC · multi-user (v2.0) · feedback hub · BI dashboard · Snowflake (conditional) | ⬜ (documented scale-paths) |

> After each release ships: tag it, append its **Produces** to [interface-contracts](interface-contracts.md), run the [migration-decision protocol](../03-roadmap.md#the-migration-decision-protocol-how-the-next-step-is-actually-chosen), and re-confirm/replace the next row here.
